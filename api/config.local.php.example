<?php
/**
 * Copy to config.local.php for local XAMPP development.
 *
 * Railway: do not use this file. Add a MySQL plugin to your project, then on your
 * web service set reference variables (Variables → New Variable):
 *   DB_HOST=${{MySQL.MYSQLHOST}}
 *   DB_PORT=${{MySQL.MYSQLPORT}}
 *   DB_USER=${{MySQL.MYSQLUSER}}
 *   DB_PASS=${{MySQL.MYSQLPASSWORD}}
 *   DB_NAME=${{MySQL.MYSQLDATABASE}}
 * Or rely on the default MYSQLHOST, MYSQLUSER, etc. when both services share a project.
 */

$DB_HOST = '127.0.0.1';
$DB_PORT = 3306;
$DB_USER = 'root';
$DB_PASS = '';
$DB_NAME = 'ctulibrarysystem';
